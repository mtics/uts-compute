#!/bin/bash
#PBS -N {{run_id}}
#PBS -q gpuq
#PBS -l ncpus={{ncpus}}
#PBS -l mem={{memory_gb}}gb
#PBS -l walltime={{walltime}}
#PBS -l ngpus={{ngpus}}
#PBS -o {{log_dir}}/{{run_id}}.out
#PBS -e {{log_dir}}/{{run_id}}.err

set -euo pipefail

mkdir -p "{{workdir}}"
cd "{{workdir}}"
{{command}}
