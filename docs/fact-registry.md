# Fact Registry

Each fact used by implementation or Skills should be recorded here with a source and stability class.

Stability classes:

- stable: unlikely to change frequently;
- dynamic: may change daily or by account;
- policy: must be checked against official documentation before high-impact automation.

| ID | Fact | Source | Last verified | Stability | Used by |
| --- | --- | --- | --- | --- | --- |
| F001 | Codex and Claude Code both support Skills and MCP concepts, but their plugin wrappers differ. | Codex and Claude Code official docs | 2026-06-15 | dynamic | `docs/architecture.md`, plugin manifests |
| F002 | UTS HPC is a PBS Professional batch cluster. | https://hpc.research.uts.edu.au/pbs/ | 2026-06-15 | stable | HPC adapter, PBS templates |
| F003 | UTS HPC queue info can be queried with `qstat -Q` and `qstat -Qf`. | https://hpc.research.uts.edu.au/pbs/queues/ | 2026-06-15 | stable | quota refresh, queue status |
| F004 | UTS HPC node info can be queried with `pbsnodes -a`, and JSON output should be preferred when available. | https://hpc.research.uts.edu.au/pbs/nodes/ | 2026-06-15 | stable | node inventory |
| F005 | UTS HPC GPU jobs request `ngpus` and target a GPU queue. The LIVE cluster exposes `small_gpuq` / `med_gpuq` / `large_gpuq` (qstat-observed); there is NO bare routing `gpuq` in live snapshots (the old `gpuq` value made `jobs.plan` disagree with submit-time conformance). The planner respects an explicit GPU queue and defaults a queue-less GPU job to `small_gpuq`. GPU jobs MUST use the PBS chunk/`select` syntax `-l select=1:ncpus=N:ngpus=N:mem=Ngb` (running job 4612: `-q small_gpuq -l select=1:ncpus=12:ngpus=2:mem=48gb`); the old separate `-l ngpus=` never binds the request to a GPU vnode → the job queues forever. The GPU template renders `select`. Agents should avoid GPU requests unless code uses GPU. | https://hpc.research.uts.edu.au/gpu/job_submit/ | 2026-06-27 | policy | safety gates, GPU template |
| F006 | UTS HPC public queue status endpoint returned an HTML PBS queue snapshot. | https://hpc.research.uts.edu.au/statuspbs/queues | 2026-06-15 | dynamic | docs snapshot only |
| F007 | UTS iHPC documentation entry returned `HTTP 200 OK` from this machine. | https://ihpc.research.uts.edu.au/help/documentation-rhel-810/ | 2026-06-15 | dynamic | docs access basis |
| F008 | iHPC is an interactive node environment using an access host and compute nodes, not PBS. | iHPC SSH and node docs | 2026-06-15 | stable | iHPC adapter, Skills |
| F009 | iHPC public node limits vary by group and must be paired with live account status. | https://ihpc.research.uts.edu.au/help/about/node-limits/ | 2026-06-15 | dynamic | profile validation |
| F010 | The user has two accounts on each UTS platform. | User statement | 2026-06-15 | dynamic | profile model |
| F011 | Multi-account support must not be designed as quota bypass or fair-use evasion. | User requirement plus platform policy risk | 2026-06-15 | policy | safety gates, account policy |
| F012 | M1 Local Dry-Run MCP is local-only and does not use SSH, PBS commands, iHPC commands, rsync execution, VPN probes, or UTS remote hosts. | Implementation in `mcp-server/src/` and tests | 2026-06-15 | stable | M1 tools, tests |
| F013 | Codex Skills load detailed instructions only when triggered and require `SKILL.md` with `name` and `description`. | https://developers.openai.com/codex/skills | 2026-06-15 | dynamic | Skill design |
| F014 | Codex plugin component paths should be plugin-root relative and begin with `./`. | https://developers.openai.com/codex/plugins/build | 2026-06-15 | dynamic | plugin manifests |
| F015 | Claude Code supports plugin-provided Skills and MCP server configuration from plugin manifests. | https://code.claude.com/docs/en/plugins-reference | 2026-06-15 | dynamic | plugin manifests |
| F016 | MCP tools should validate inputs, sanitize outputs, enforce access controls, and distinguish tool execution errors from protocol errors. | https://modelcontextprotocol.io/specification/2025-06-18/server/tools | 2026-06-15 | dynamic | MCP server design |
| F017 | MCP resources and prompts are distinct capabilities for reusable context and user-controlled workflow templates. | https://modelcontextprotocol.io/specification/2025-06-18/server/resources and https://modelcontextprotocol.io/specification/2025-06-18/server/prompts | 2026-06-15 | dynamic | future MCP resources/prompts |
| F018 | M2a `access.check` is implemented as profile-scoped read-only preflight with mocked default tests and redacted local evidence under `.uts-computing/access`. | Implementation in `mcp-server/src/core/access.ts` and `mcp-server/tests/core/access.test.mjs` | 2026-06-15 | stable | M2a access checks |
| F019 | M2b `quotas.refresh` is implemented for UTS HPC profiles with mocked default tests and redacted local evidence under `.uts-computing/quotas`. | Implementation in `mcp-server/src/ops/quotas/quotas.ts` and `mcp-server/tests/ops/quotas.test.mjs` | 2026-06-15 | stable | M2b HPC quota refresh |
| F020 | M2c `quotas.refresh` is implemented for UTS iHPC profiles with mocked default tests and redacted local evidence under `.uts-computing/quotas`. | Implementation in `mcp-server/src/ops/quotas/quotas.ts` and `mcp-server/tests/ops/quotas.test.mjs` | 2026-06-15 | stable | M2c iHPC quota refresh |
| F021 | M2d MCP resources expose redacted profiles, templates, sanitized quota snapshots, run records, and allowlisted local docs without live side effects. | Implementation in `mcp-server/src/mcp/resources.ts` and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M2d MCP resources |
| F022 | M3a approval records are implemented as local state bound to deterministic `plan_hash` values and fresh quota snapshots, with trusted-token decisions. | Implementation in `mcp-server/src/ops/plans/planner.ts`, `mcp-server/src/ops/approvals/approvals.ts`, and `mcp-server/tests/ops/approval.test.mjs` | 2026-06-15 | stable | M3a approval state |
| F023 | M3b `jobs.submit` is implemented for UTS HPC PBS plans with saved plan artifacts, approved approval consumption, and mocked default qsub submission tests. | Implementation in `mcp-server/src/ops/jobs/submit.ts`, `mcp-server/src/ops/plans/plan-store.ts`, and `mcp-server/tests/ops/submit.test.mjs` | 2026-06-15 | stable | M3b HPC submission |
| F024 | The UTS HPC PBS side of M3d is implemented with `jobs.status`, `jobs.logs`, and `jobs.cancel` using fixed qstat/tail/qdel adapters, plan-derived log paths, operation-specific cancellation approvals, and mocked default tests. | Implementation in `mcp-server/src/ops/jobs/jobs.ts` and `mcp-server/tests/ops/jobs.test.mjs` | 2026-06-15 | stable | M3d HPC monitor/cancel |
| F025 | M3c `jobs.submit` is implemented for UTS iHPC supervised starts with saved `command_argv`, active `cnode mynodes` evidence, a fixed Python supervisor, persisted supervisor metadata, and mocked default start tests. | Implementation in `mcp-server/src/ops/jobs/ihpc-start.ts`, `mcp-server/src/ops/plans/command-argv.ts`, and `mcp-server/tests/ops/ihpc-start.test.mjs` | 2026-06-15 | stable | M3c iHPC supervised start |
| F026 | The UTS iHPC portion of M3d is implemented with `jobs.status`, `jobs.logs`, and `jobs.cancel` using persisted supervisor pid/node/log metadata, fixed Python helper adapters, operation-specific cancellation approvals, and mocked default tests. | Implementation in `mcp-server/src/ops/jobs/jobs.ts` and `mcp-server/tests/ops/ihpc-jobs.test.mjs` | 2026-06-15 | stable | M3d iHPC monitor/cancel |
| F027 | M3e approval-policy hardening is implemented with shared server-side plan risk reasons, operation-specific approval reasons, optional cross-account switch context, approval reuse checks, and mocked/default protocol tests. | Implementation in `mcp-server/src/ops/approvals/approval-policy.ts`, `mcp-server/src/ops/approvals/approvals.ts`, `mcp-server/tests/ops/approval.test.mjs`, `mcp-server/tests/ops/submit.test.mjs`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M3e approval policy |
| F028 | M4 first-slice artifact handling is implemented with plan-derived artifact listing, manifest-based single-file fetch, SHA-256 verification, local summary generation, dry-run cleanup planning, scoped cleanup execution, operation-specific artifact-fetch/cleanup approvals, symlink-boundary checks in fixed Python helpers, and mocked/default protocol tests. | Implementation in `mcp-server/src/ops/data/artifacts.ts`, `mcp-server/src/ops/approvals/approvals.ts`, `mcp-server/tests/ops/artifacts.test.mjs`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M4 artifacts |
| F029 | M5 first-slice packaging and hardening is implemented with shared Codex/Claude plugin validation, plugin-root-relative path checks, schema migration planning, failure playbooks, expanded redaction tests, and mocked PBS failure-path tests. | Implementation in `scripts/validate-plugin-package.mjs`, `mcp-server/tests/integration/packaging.test.mjs`, `mcp-server/tests/core/redaction.test.mjs`, `mcp-server/tests/ops/jobs.test.mjs`, `docs/plugin-setup.md`, `docs/schema-migration-plan.md`, and `docs/failure-playbooks.md` | 2026-06-15 | stable | M5 packaging hardening |
| F030 | Artifact manifests, single/batch artifact fetch evidence, artifact summaries, cleanup plans, and cleanup execution records now have dedicated schemas and sanitized MCP resources expose artifact state without raw remote paths, local paths, fetched content, raw cleanup helper args, helper output, or `files/...` browsing. | Implementation in `schemas/artifact-*.schema.json`, `mcp-server/src/core/validation.ts`, `mcp-server/src/mcp/resources.ts`, `mcp-server/tests/ops/artifacts.test.mjs`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M4 artifact resources |
| F031 | MCP tools now return a consistent JSON envelope through both text content and `structuredContent`; expected business failures use tool results with `isError: true`, and protocol tests cover iHPC `jobs.status`, `jobs.logs`, and `jobs.cancel` schemas plus calls without live SSH. | Implementation in `mcp-server/src/index.ts` and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M5 protocol hardening |
| F032 | `artifacts.fetch.batch` is implemented for explicit manifest file ids with required `manifest_hash`, per-file and total byte limits, operation-specific approval scope hashes, pre-command manifest total rejection, and mocked/default protocol inventory tests. | Implementation in `mcp-server/src/ops/data/artifacts.ts`, `mcp-server/src/ops/approvals/approvals.ts`, `mcp-server/tests/ops/artifacts.test.mjs`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M4 batch artifact fetch |
| F033 | `transfers.execute` is implemented for saved fixed-file transfer plans with deterministic `plan_hash`, scoped `transfers.execute` approvals, preflighted file lists, helper metadata checks that require exact saved-file, byte-accounting, and checksum-policy matches, bounded SHA-256 verification for checksum-eligible upload/download files, explicit `skipped-large` evidence for oversized files, download post-rsync local destination verification, total byte limits, fixed `rsync --files-from=-` argv, execution evidence schema validation, and mocked/default protocol tests. | Implementation in `mcp-server/src/ops/data/transfer.ts`, `mcp-server/src/ops/approvals/approvals.ts`, `schemas/transfer-execution-record.schema.json`, `mcp-server/tests/ops/transfers.test.mjs`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M4 fixed-file transfer |
| F034 | `state.migrate.plan` is implemented as a read-only local-state schema migration dry-run tool. Persisted record schemas, including planned job and planned transfer artifacts, accept optional `schema_version: "0.1.0"`; the tool reports future apply candidates and blockers without writing files, changing approval/run state, or contacting UTS systems. | Implementation in `mcp-server/src/ops/data/migrations.ts`, `schemas/*.schema.json`, `mcp-server/tests/ops/migrations.test.mjs`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M5 schema migration |
| F035 | Sanitized transfer MCP resources are implemented for transfer state indexes, aggregate transfer state, saved transfer plans, and execution evidence. They expose only allowlisted checksum evidence fields when present and hide raw scripts, raw local paths, host aliases, helper output, and broad transfer file browsing. | Implementation in `mcp-server/src/mcp/resources.ts`, `schemas/planned-transfer.schema.json`, `schemas/transfer-execution-record.schema.json`, `mcp-server/tests/integration/mcp-protocol.test.mjs`, and `mcp-server/tests/ops/transfers.test.mjs` | 2026-06-15 | stable | M4 transfer resources |
| F036 | `jobs.retry.plan` is implemented as a local-only retry dry-run tool. Retry-derived plan/run records carry `retry_of` lineage and `approval_operation: "jobs.retry"`; UTS HPC retry submission and UTS iHPC supervised retry start require matching `jobs.retry` approval plus plan/run lineage checks before command execution. | Implementation in `mcp-server/src/ops/jobs/retry.ts`, `mcp-server/src/ops/jobs/submit.ts`, `mcp-server/src/ops/jobs/ihpc-start.ts`, `mcp-server/src/ops/approvals/submission-approval.ts`, `mcp-server/src/ops/approvals/approvals.ts`, `schemas/planned-job.schema.json`, `schemas/run-record.schema.json`, `mcp-server/tests/ops/retry.test.mjs`, `mcp-server/tests/ops/ihpc-start.test.mjs`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M3f retry planning |
| F037 | `state.migrate.apply` is implemented as a confirmed local-only additive schema-version migration. It requires the current `state.migrate.plan` hash plus a trusted token, rejects blockers before writing, backs up each changed file, and preserves approval/run state while adding only `schema_version` metadata. | Implementation in `mcp-server/src/ops/data/migrations.ts`, `mcp-server/src/index.ts`, `schemas/state-migration-*.schema.json`, `mcp-server/tests/ops/migrations.test.mjs`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M5 schema migration apply |
| F038 | `docs.search` is implemented as a read-only search over allowlisted local project documentation. It returns bounded snippets and `uts://docs/{docId}` references without accepting paths, URLs, live-fetch flags, or runtime roots. | Implementation in `mcp-server/src/ops/catalog/docs.ts`, `mcp-server/src/index.ts`, `mcp-server/src/mcp/resources.ts`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M2 docs search |
| F039 | `docs.refresh` is implemented as a fixed-source official UTS documentation cache tool. It writes schema-validated sanitized cache records under `.uts-computing/docs-cache`, exposes `uts://docs-cache/{sourceId}`, and does not accept arbitrary URLs, paths, hosts, headers, profile ids, proxy settings, credentials, or output directories. | Implementation in `mcp-server/src/ops/catalog/docs.ts`, `mcp-server/src/index.ts`, `mcp-server/src/mcp/resources.ts`, `schemas/docs-cache-record.schema.json`, `mcp-server/tests/ops/docs.test.mjs`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M2 docs refresh |
| F040 | `artifacts.summarize` now extracts bounded local metrics from allowlisted JSON, JSONL, NDJSON, CSV, and TSV filenames whose safe stem tokens include metric/result/summary/eval/score terms. It skips symlinks, oversized files, non-metric logs/text, secret-like filenames, secret-like keys, and raw table embedding while preserving local-only operation. | Implementation in `mcp-server/src/ops/data/artifacts.ts` and `mcp-server/tests/ops/artifacts.test.mjs` | 2026-06-15 | stable | M4 artifact summary |
| F041 | `npm run smoke:plugin` is implemented as a host-neutral direct MCP stdio plugin smoke test. It validates the shared Codex/Claude shims, launches the MCP server from `.mcp.json` inside a temporary plugin root, forces example profiles, runs offline profiles/templates/prompts/state migration/jobs.plan checks, and avoids VPN, SSH, UTS hosts, live submission, transfer, artifact fetch, and migration apply tools. | Implementation in `scripts/host-neutral-plugin-smoke.mjs`, `scripts/validate-plugin-package.mjs`, `package.json`, `mcp-server/tests/integration/packaging.test.mjs`, and `mcp-server/tests/integration/plugin-smoke-boundary.test.mjs` | 2026-06-15 | stable | M5 plugin smoke |
| F042 | Real Codex and Claude Code installed-client smoke evidence now has a fixed prompt generator, JSON schema, validator, and tests. Actual evidence files are still dynamic release artifacts that must be collected inside each client host before release. | Implementation in `scripts/client-installed-smoke.mjs`, `scripts/validate-client-smoke-evidence.mjs`, `schemas/client-smoke-evidence.schema.json`, `docs/client-installed-smoke-evidence.md`, and `mcp-server/tests/integration/client-smoke-evidence.test.mjs` | 2026-06-15 | dynamic | M5 client smoke |
| F043 | MCP workflow prompts are implemented for experiment planning, run triage, artifact collection, fixed-file transfer staging, and installed-client smoke evidence. Prompt handlers return guidance only, redact sensitive user prompt arguments, write no local state, and are statically guarded from importing live/state/action modules. | Implementation in `mcp-server/src/mcp/prompts.ts`, `mcp-server/src/index.ts`, `mcp-server/tests/integration/mcp-protocol.test.mjs`, `mcp-server/tests/integration/prompts.test.mjs`, and `scripts/host-neutral-plugin-smoke.mjs` | 2026-06-15 | stable | M5 MCP prompts |
| F044 | `artifacts.cleanup.execute` is implemented only for terminal runs and explicit latest-manifest file artifact ids with captured SHA-256 and size evidence, exact `artifacts.cleanup.execute` approval scope, fixed unlink-regular-files-only helpers, local cache realpath/symlink checks, execution evidence schema validation, and sanitized cleanup evidence resources. It does not implement raw-path, directory, glob, or recursive cleanup. | Implementation in `mcp-server/src/ops/data/artifacts.ts`, `mcp-server/src/ops/approvals/approvals.ts`, `mcp-server/src/mcp/resources.ts`, `schemas/artifact-cleanup-execution-record.schema.json`, `mcp-server/tests/ops/artifacts.test.mjs`, and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M4 artifact cleanup execute |
| F045 | MCP tool input schemas are strict at the top level. Protocol tests verify dangerous extra arguments such as raw paths, URLs, hosts, commands, remote job ids, globs, force flags, execution-time file lists, and runtime roots are rejected by input validation before handlers run. | Implementation in `mcp-server/src/index.ts` and `mcp-server/tests/integration/mcp-protocol.test.mjs` | 2026-06-15 | stable | M5 protocol hardening |
| F048 | ADR 0005 standards-first distribution: the Codex plugin wrapper is retired. Verified (2026) that Codex consumes open standards directly — it registers a stdio MCP server via `codex mcp add` / `~/.codex/config.toml` independent of any plugin, and auto-discovers `SKILL.md` folders from `.agents/skills/` (agentskills.io open standard, also read by Cursor/Copilot/Gemini CLI). So `.codex-plugin/plugin.json` and `.codex.mcp.json` were removed; `.agents/skills/` now mirrors `skills/` via symlinks. Only Claude Code keeps a plugin (`.claude-plugin/` + `marketplace.json` + `.mcp.json` with `${CLAUDE_PLUGIN_ROOT}`). `validate:plugin` now checks the Claude plugin + that every `skills/<name>` is mirrored under `.agents/skills/`. There is no ratified cross-vendor plugin standard in 2026; unified-looking install pages (Smithery, registry UIs) generate per-client config under the hood. This supersedes the Codex-plugin parts of F014/F029/F047 and ADR 0001/0003. | Implementation in `.agents/skills/`, `.claude-plugin/`, `.mcp.json`, `scripts/validate-plugin-package.mjs`, `scripts/host-neutral-plugin-smoke.mjs`, `docs/adr/0005-standards-first-distribution.md`, `docs/plugin-setup.md`, and `mcp-server/tests/integration/packaging.test.mjs` | 2026-06-16 | stable | ADR 0005 standards-first distribution |
| F047 | The plugin MCP config is client-specific so the server launches regardless of the client's working directory (a bare relative launch arg was proven to fail from a non-plugin-root CWD). `.claude-plugin/plugin.json` → `./.mcp.json` with `args: ["${CLAUDE_PLUGIN_ROOT}/mcp-server/dist/index.js"]`; `.codex-plugin/plugin.json` → `./.codex.mcp.json` with `args: ["${PLUGIN_ROOT}/mcp-server/dist/index.js"]`. The two configs are otherwise identical; `validate:plugin` checks each against its expected plugin-root variable. The server still resolves its relative `UTS_COMPUTING_CONFIG` against its own location (`import.meta.url`), so the env path stays plugin-root-relative. Codex `${PLUGIN_ROOT}` substitution inside MCP config is undocumented (confirmed only for hook commands), so it is validated structurally and must be confirmed once in a real installed-Codex smoke. This refines F014/F029 and ADR 0003. | Implementation in `.mcp.json`, `.codex.mcp.json`, `.codex-plugin/plugin.json`, `scripts/validate-plugin-package.mjs`, `scripts/host-neutral-plugin-smoke.mjs`, `scripts/client-installed-smoke.mjs`, `scripts/validate-client-smoke-evidence.mjs`, `schemas/client-smoke-evidence.schema.json`, `docs/adr/0003-plugin-shim-contract.md`, and `mcp-server/tests/integration/packaging.test.mjs` | 2026-06-16 | stable | ADR 0003 client-specific MCP config |
| F046 | ADR 0004 quota-envelope autonomy is implemented. `jobs.submit`, `jobs.retry`, `transfers.execute`, `artifacts.fetch`, `artifacts.fetch.batch`, and iHPC supervised start no longer require a human token: `approvalId` is optional and they run autonomously, gated by live conformance against the selected profile's fresh quota snapshot — PBS queue enable/ACL/`resources_max`/per-user `max_run`/`max_queued` (`checkPbsConformance`), iHPC active-cnode for the planned node-family (`selectActiveComputeNode`), and target-mount storage headroom from parsed `df` (`parseDfAvailable` + `checkStorageHeadroom`). Records omit `approval_id` when autonomous and record `not_required` authorization with the snapshot id. `approval-policy.ts` thresholds are advisory display-only (`approval.policy: "advisory"`). `artifacts.cleanup.execute`, `jobs.cancel`, and `state.migrate.apply` keep the token. This supersedes the token gates noted in F023/F025/F032/F033 for those operations. **iHPC-specific (ban-critical):** a UTS iHPC supervised start (and iHPC supervised retry start) ALWAYS requires a fresh `quotaSnapshotId` — even on the approval path — because the per-account node-pool gate (`checkIhpcNodePoolConformance` over `snapshotActiveNodes` from `readFreshQuotaSnapshot`) must read held-node occupancy from consume-time-fresh evidence, never an approval's bound snapshot (an approval may live up to 24h while its bound snapshot is trusted for held-node evidence only within the 15-min freshness window). An approval-gated iHPC submit therefore requires BOTH the `approvalId` AND a fresh `quotaSnapshotId`; an iHPC submit with only an `approvalId` hard-fails ("iHPC supervised start with an approval still requires a fresh quotaSnapshotId for the ban-critical node-pool gate"). The PBS approval-only path is unchanged. | Implementation in `mcp-server/src/ops/quotas/conformance.ts`, `mcp-server/src/ops/quotas/quota-limits.ts`, `mcp-server/src/ops/jobs/submit.ts`, `mcp-server/src/ops/jobs/ihpc-start.ts`, `mcp-server/src/ops/data/transfer.ts`, `mcp-server/src/ops/data/artifacts.ts`, `docs/adr/0004-quota-envelope-autonomy.md`, and tests `ops/conformance.test.mjs`, `ops/storage-headroom.test.mjs`, `ops/submit-autonomous.test.mjs`, `ops/redaction-roots.test.mjs`, `ops/approval-advisory.test.mjs`, `ops/ihpc-start.test.mjs` | 2026-06-16 | stable | ADR 0004 autonomy |
| F049 | Feature-migration backlog (research over same-domain HPC/MCP peers) added 5 MCP tools (26 to 31) plus reproducibility capture: `jobs.history` (cross-run listing), `access.doctor` (live connectivity + clock-skew + PBS-reachability health check), `jobs.usage` (PBS core/GPU-hour and CPU-efficiency accounting from `qstat -x`), `jobs.diagnose` (failure classification over status + bounded log tail via the failure-playbooks taxonomy), `sweep.plan` (hyperparameter grid into one PBS array plan + index-to-params table), and an optional `reproducibility` block (git sha/branch/dirty + redacted command) on the run record. All read-only or dry-run; dot-notation tool names kept deliberately. | Implementation in `mcp-server/src/ops/jobs/history.ts`, `ops/access/doctor.ts`, `ops/jobs/accounting.ts`, `ops/jobs/diagnose.ts`, `ops/jobs/sweep.ts`, `ops/plans/reproducibility.ts`, `ops/jobs/jobs.ts` (getJobUsage), `index.ts`, and tests `ops/jobs-history`/`ops/doctor`/`ops/accounting`/`ops/jobs-usage`/`ops/diagnose`/`ops/sweep`/`ops/reproducibility`/`integration/mcp-protocol`.test.mjs | 2026-06-16 | stable | feature migration |
